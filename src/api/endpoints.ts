type EndpointsType = {
  auth: "/auth";
  sites: "/sites";
  requests: "/requests";
  stats: "/stats"
  targets: "/targets",
  auditor: "/auditor",
  ai: "/ai"

};

const ENDPOINTS: EndpointsType = {
  auth: "/auth",
  sites: "/sites",
  requests: "/requests",
  stats: "/stats",
  targets: "/targets",
  auditor: "/auditor",
  ai: "/ai"
};

export default ENDPOINTS;
