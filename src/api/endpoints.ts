type EndpointsType = {
  auth: "/auth";
  sites: "/sites";
  requests: "/requests";
  stats: "/stats"

};

const ENDPOINTS: EndpointsType = {
  auth: "/auth",
  sites: "/sites",
  requests: "/requests",
  stats: "/stats"
};

export default ENDPOINTS;
