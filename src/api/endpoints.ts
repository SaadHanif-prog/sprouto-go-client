type EndpointsType = {
  auth: "/auth";
  sites: "/sites";
  requests: "/requests";
  stats: "/stats"
  targets: "/targets",
  auditor: "/auditor"

};

const ENDPOINTS: EndpointsType = {
  auth: "/auth",
  sites: "/sites",
  requests: "/requests",
  stats: "/stats",
  targets: "/targets",
  auditor: "/auditor"
};

export default ENDPOINTS;
