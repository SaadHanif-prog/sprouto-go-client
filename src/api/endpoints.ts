type EndpointsType = {
  auth: "/auth";
  sites: "/sites";
  requests: "/requests";

};

const ENDPOINTS: EndpointsType = {
  auth: "/auth",
  sites: "/sites",
  requests: "/requests"
};

export default ENDPOINTS;
